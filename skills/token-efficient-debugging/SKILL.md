---

name: token-efficient-debugging

description: Use when debugging builds, tests, lint errors or large logs while minimizing context usage.

---

When debugging:

1. Search before reading entire files.

2. Run verbose commands through tkrun.

3. Keep full logs on disk.

4. Feed only relevant failures into context.

5. Read additional log ranges only when necessary.

